# GPIO Libraries
# RECOMMENDED: Install via apt for better compatibility
#   sudo apt install python3-lgpio python3-libgpiod python3-rpi-lgpio
#
# python3-rpi-lgpio provides RPi.GPIO API compatibility using lgpio backend
# This is the recommended approach for new projects on Raspberry Pi OS Bookworm
#
# If you must use pip (not recommended), uncomment below:
# RPi.GPIO>=0.7.1  # Legacy library (deprecated on Bookworm)

# Computer Vision
opencv-python>=4.8.0
imutils>=0.5.4

# Thermal Camera (FLIR Lepton)
numpy>=1.24.0
pygame>=2.5.0
scipy>=1.10.0
colour>=0.1.5

# CCTV Application
PyQt6>=6.5.0
qdarkstyle>=3.1

# Optional: Camera interface (if using legacy picamera)
# picamera>=1.13
