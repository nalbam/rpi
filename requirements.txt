# GPIO Libraries (installed via apt: python3-lgpio python3-gpiod)
# RPi.GPIO is legacy but still supported for backwards compatibility
RPi.GPIO>=0.7.1

# Computer Vision
opencv-python>=4.8.0
imutils>=0.5.4

# Thermal Camera (FLIR Lepton)
numpy>=1.24.0
pygame>=2.5.0
scipy>=1.10.0
colour>=0.1.5

# CCTV Application
PyQt6>=6.5.0
qdarkstyle>=3.1

# Optional: Camera interface (if using legacy picamera)
# picamera>=1.13
